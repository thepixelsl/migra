---
import { Image } from "astro:assets";
import MobileNavigation from "../../components/MobileNavigation.astro";
import anchorImage from "../../assets/impressum/anker-york-augustin.jpg";
import "../../styles/global.css";

const siteUrl = "https://artbild-fotografie.de";
const pageUrl = `${siteUrl}/kontakt/`;
const title = "Kontakt | Hochzeitsfotograf Hamburg anfragen";
const description =
  "Kontaktformular für Hochzeitsfotografie in Hamburg: freie Termine, Hochzeitsreportagen, Standesamt, Getting Ready und Paarshootings bei Artbild-Fotografie anfragen.";
const logoUrl = `${siteUrl}/images/logo-artbild-black.png`;
const primaryImage = new URL(anchorImage.src, siteUrl).href;

const schemaGraph = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "ContactPage",
      "@id": `${pageUrl}#webpage`,
      url: pageUrl,
      name: title,
      description,
      dateModified: "2026-07-02T10:00:00+02:00",
      isPartOf: {
        "@id": `${siteUrl}/#website`,
      },
      primaryImageOfPage: {
        "@type": "ImageObject",
        url: primaryImage,
        width: anchorImage.width,
        height: anchorImage.height,
        caption: "Kontakt mit Artbild-Fotografie Hamburg",
      },
      contentLocation: {
        "@type": "City",
        name: "Hamburg",
        address: {
          "@type": "PostalAddress",
          addressLocality: "Hamburg",
          addressRegion: "Hamburg",
          addressCountry: "DE",
        },
      },
      breadcrumb: {
        "@id": `${pageUrl}#breadcrumb`,
      },
      mainEntity: {
        "@type": "ContactPoint",
        contactType: "customer support",
        email: "info@artbild-fotografie.de",
        areaServed: [
          { "@type": "City", name: "Hamburg" },
          { "@type": "AdministrativeArea", name: "Norddeutschland" },
          { "@type": "Country", name: "Deutschland" },
        ],
        availableLanguage: ["de", "en"],
      },
      inLanguage: "de",
    },
    {
      "@type": "ProfessionalService",
      "@id": `${siteUrl}/#organization`,
      name: "Artbild-Fotografie",
      legalName: "Artbild-Fotografie",
      url: siteUrl,
      logo: {
        "@type": "ImageObject",
        url: logoUrl,
      },
      email: "info@artbild-fotografie.de",
      founder: {
        "@type": "Person",
        name: "York Augustin",
      },
      address: {
        "@type": "PostalAddress",
        streetAddress: "Rahlstedter Bahnhofstraße 27",
        postalCode: "22143",
        addressLocality: "Hamburg",
        addressRegion: "Hamburg",
        addressCountry: "DE",
      },
      areaServed: [
        { "@type": "City", name: "Hamburg" },
        { "@type": "AdministrativeArea", name: "Norddeutschland" },
        { "@type": "Country", name: "Deutschland" },
      ],
      serviceType: [
        "Hochzeitsfotografie",
        "Hochzeitsreportage",
        "Standesamtliche Trauung Hamburg",
        "Portraitshooting",
        "TFP Shooting",
      ],
      sameAs: [
        "https://www.instagram.com/artbild/",
        "https://www.facebook.com/artbildfotografie/",
        "https://www.pinterest.de/artbildf/",
      ],
    },
    {
      "@type": "WebSite",
      "@id": `${siteUrl}/#website`,
      url: siteUrl,
      name: "Artbild-Fotografie",
      publisher: {
        "@id": `${siteUrl}/#organization`,
      },
      inLanguage: "de",
    },
    {
      "@type": "BreadcrumbList",
      "@id": `${pageUrl}#breadcrumb`,
      itemListElement: [
        {
          "@type": "ListItem",
          position: 1,
          name: "Startseite",
          item: siteUrl,
        },
        {
          "@type": "ListItem",
          position: 2,
          name: "Kontakt",
          item: pageUrl,
        },
      ],
    },
  ],
};
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{title}</title>
    <meta name="description" content={description}>
    <meta
      name="robots"
      content="follow, index, max-snippet:-1, max-video-preview:-1, max-image-preview:large"
    >
    <link rel="canonical" href={pageUrl}>
    <meta property="og:locale" content="de_DE">
    <meta property="og:type" content="website">
    <meta property="og:title" content={title}>
    <meta property="og:description" content={description}>
    <meta property="og:url" content={pageUrl}>
    <meta property="og:site_name" content="Artbild-Fotografie">
    <meta property="og:image" content={primaryImage}>
    <meta property="og:image:width" content={anchorImage.width}>
    <meta property="og:image:height" content={anchorImage.height}>
    <meta property="og:image:alt" content="Kontakt mit Artbild-Fotografie Hamburg">
    <meta name="twitter:card" content="summary_large_image">
    <script type="application/ld+json" set:html={JSON.stringify(schemaGraph)}></script>
  </head>
  <body class="contact-page">
    <header class="gallery-header">
      <MobileNavigation />
      <nav class="gallery-nav" aria-label="Hauptnavigation">
        <ul class="gallery-nav__group">
          <li><a href="/">Start</a></li>
          <li><a href="/portfolio/">Portfolio</a></li>
          <li><a href="/kontakt/" aria-current="page">Kontakt</a></li>
        </ul>
        <a class="gallery-logo" href="/" aria-label="Artbild-Fotografie Startseite">
          <img
            src="/images/logo-artbild-black.png"
            width="1200"
            height="480"
            alt="Artbild Fotografie"
          >
        </a>
        <ul class="gallery-nav__group">
          <li><a href="/blog/">Blog</a></li>
          <li><a href="/about/">Über mich</a></li>
          <li><a href="/newsletter/">Newsletter</a></li>
          <li>
            <a class="gallery-search" href="/?s=" aria-label="Suche">⌕</a>
          </li>
        </ul>
      </nav>
    </header>

    <main id="main-content">
      <nav class="contact-breadcrumb" aria-label="Breadcrumb">
        <a href="/">Startseite</a>
        <span aria-hidden="true">/</span>
        <span aria-current="page">Kontakt</span>
      </nav>

      <section class="contact-hero" aria-labelledby="contact-title">
        <div class="contact-hero__copy">
          <p class="contact-kicker">Anfrage & freie Termine</p>
          <h1 id="contact-title">Kontakt zu Artbild-Fotografie</h1>
          <p>
            Erzählt mir kurz von Eurer Hochzeit, dem Ort und dem gewünschten
            Datum. Ich melde mich persönlich zurück und prüfe, ob eine
            Hochzeitsreportage in Hamburg, Norddeutschland oder darüber hinaus
            möglich ist.
          </p>
        </div>
        <figure class="contact-hero__image">
          <Image
            src={anchorImage}
            widths={[420, 640, 860, 1100]}
            sizes="(max-width: 820px) 100vw, 38vw"
            format="webp"
            quality={88}
            loading="eager"
            fetchpriority="high"
            decoding="async"
            alt="York Augustin von Artbild-Fotografie in Hamburg"
          />
        </figure>
      </section>

      <section class="contact-shell" aria-labelledby="contact-form-title">
        <div class="contact-aside" aria-label="Direkte Kontaktwege">
          <p class="contact-kicker">Direkt</p>
          <h2>Für kurze Wege</h2>
          <p>
            Für kurzfristige Hochzeiten ist eine knappe Nachricht mit Datum,
            Ort und gewünschter Begleitdauer am hilfreichsten.
          </p>
          <dl>
            <div>
              <dt>E-Mail</dt>
              <dd>
                <a href="mailto:info@artbild-fotografie.de">info@artbild-fotografie.de</a>
              </dd>
            </div>
            <div>
              <dt>WhatsApp</dt>
              <dd>
                <a
                  href="https://wa.me/4915161438120?text=Hallo%20York%2C%20ich%20m%C3%B6chte%20eine%20Hochzeit%20anfragen."
                  rel="noopener"
                >
                  Kurznachricht senden
                </a>
              </dd>
            </div>
            <div>
              <dt>Instagram</dt>
              <dd>
                <a href="https://ig.me/m/artbild" rel="noopener">artbild kontaktieren</a>
              </dd>
            </div>
          </dl>
          <a class="contact-secure-link" href="/sicherer-kontakt/">
            Sicherer Kontakt mit PGP und S/MIME
          </a>
        </div>

        <form
          class="contact-form"
          action="/api/contact"
          method="post"
          enctype="multipart/form-data"
          aria-labelledby="contact-form-title"
          aria-describedby="contact-form-intro contact-form-status"
          data-contact-form
        >
          <input type="hidden" name="source_path" value="/kontakt/">
          <input type="hidden" name="form_loaded_at" value="" data-contact-loaded-at>
          <input type="hidden" name="js_enabled" value="0" data-contact-js>

          <div class="contact-form__header">
            <p class="contact-kicker">Formular</p>
            <h2 id="contact-form-title">Termin anfragen</h2>
            <p id="contact-form-intro">
              Kurz reicht: Name, E-Mail, Ort und Wunschdatum. So kann ich zuerst
              prüfen, ob der Termin überhaupt frei ist.
            </p>
          </div>

          <div class="contact-form__honeypot" aria-hidden="true">
            <label for="contact-website">Website</label>
            <input
              id="contact-website"
              name="website"
              type="text"
              tabindex="-1"
              autocomplete="off"
              data-agent-ignore="true"
            >
          </div>

          <fieldset>
            <legend>Anfrage</legend>
            <div class="contact-form__grid">
              <div class="contact-field">
                <label for="contact-request-type">Worum geht es? *</label>
                <select
                  id="contact-request-type"
                  name="request_type"
                  required
                  aria-describedby="contact-request-type-hint"
                  data-agent-field="request-type"
                  data-contact-request-type
                >
                  <option value="">Bitte auswählen</option>
                  <option value="hochzeit">Hochzeit</option>
                  <option value="standesamtliche-trauung">Standesamtliche Trauung</option>
                  <option value="portraitshooting">Portraitshooting</option>
                  <option value="tfp">TFP</option>
                </select>
                <p id="contact-request-type-hint">
                  Bei TFP-Anfragen könnt Ihr unten bis zu drei Beispielbilder anhängen.
                </p>
              </div>

              <div class="contact-field">
                <label for="contact-name">Euer Name *</label>
                <input
                  id="contact-name"
                  name="name"
                  type="text"
                  autocomplete="name"
                  required
                  minlength="2"
                  maxlength="120"
                  data-agent-field="full-name"
                >
              </div>

              <div class="contact-field">
                <label for="contact-email">E-Mail-Adresse *</label>
                <input
                  id="contact-email"
                  name="email"
                  type="email"
                  autocomplete="email"
                  inputmode="email"
                  required
                  data-agent-field="email"
                >
              </div>

              <div class="contact-field">
                <label for="contact-event-date">
                  Wunschdatum <span data-date-required-marker>*</span>
                </label>
                <input
                  id="contact-event-date"
                  name="event_date"
                  type="date"
                  autocomplete="off"
                  required
                  aria-describedby="contact-date-hint"
                  data-agent-field="event-date"
                  data-contact-date
                >
                <p id="contact-date-hint">Für TFP darf das Datum offen bleiben.</p>
              </div>

              <div class="contact-field">
                <label for="contact-location">Ort oder Location *</label>
                <input
                  id="contact-location"
                  name="location"
                  type="text"
                  autocomplete="address-level2"
                  placeholder="z. B. Hamburg, Standesamt Altona"
                  required
                  minlength="2"
                  maxlength="160"
                  data-agent-field="event-location"
                >
              </div>

              <div class="contact-field">
                <label for="contact-security-year">Welches Jahr haben wir? *</label>
                <input
                  id="contact-security-year"
                  name="security_year"
                  type="text"
                  inputmode="numeric"
                  maxlength="4"
                  placeholder="JJJJ"
                  autocomplete="off"
                  required
                  aria-describedby="contact-security-year-hint"
                  data-agent-field="current-year-check"
                  data-current-year
                >
                <p id="contact-security-year-hint">
                  Bitte das aktuelle Jahr eintragen. Die Prüfung aktualisiert sich automatisch.
                </p>
              </div>
            </div>
          </fieldset>

          <fieldset>
            <legend>Optional</legend>
            <div class="contact-field contact-field--wide">
              <label for="contact-message">Kurze Nachricht</label>
              <textarea
                id="contact-message"
                name="message"
                rows="5"
                maxlength="1200"
                aria-describedby="contact-message-hint"
                data-agent-field="message"
              ></textarea>
              <p id="contact-message-hint">
                Optional, falls etwas Besonderes wichtig ist. Bitte keine Links oder Code-Fragmente senden.
              </p>
            </div>

            <div
              class="contact-field contact-field--wide contact-field--tfp"
              hidden
              data-contact-tfp
            >
              <label for="contact-tfp-images">TFP-Bilder</label>
              <input
                id="contact-tfp-images"
                name="tfp_images"
                type="file"
                accept="image/jpeg,image/png,image/webp"
                multiple
                aria-describedby="contact-tfp-images-hint"
                data-agent-field="tfp-image-upload"
                data-contact-tfp-images
              >
              <p id="contact-tfp-images-hint">
                Maximal drei Bilder, je 2 MB. Erlaubt sind nur JPG, PNG oder WebP.
              </p>
            </div>
          </fieldset>

          <label class="contact-consent" for="contact-privacy">
            <input
              id="contact-privacy"
              name="privacy"
              type="checkbox"
              value="yes"
              required
              data-agent-field="privacy-consent"
            >
            <span>
              Ich bin damit einverstanden, dass meine Angaben zur Bearbeitung
              der Anfrage verarbeitet werden. Weitere Informationen stehen in der
              <a href="/datenschutz/">Datenschutzerklärung</a>. *
            </span>
          </label>

          <p class="contact-status" id="contact-form-status" role="status" aria-live="polite" tabindex="-1" data-contact-status></p>

          <div class="contact-actions">
            <button class="contact-submit" type="submit" data-contact-submit>
              Anfrage senden
            </button>
            <a href="mailto:info@artbild-fotografie.de?subject=Hochzeitsanfrage%20%C3%BCber%20Artbild-Fotografie">
              Direkt per E-Mail
            </a>
          </div>
        </form>
      </section>
    </main>

    <footer class="contact-footer">
      <a href="/impressum/">Impressum</a>
      <a href="/datenschutz/">Datenschutz</a>
      <a href="/sicherer-kontakt/">Sicherer Kontakt</a>
    </footer>

    <script>
      const contactForm = document.querySelector("[data-contact-form]");

      if (contactForm instanceof HTMLFormElement) {
        const loadedAt = contactForm.querySelector("[data-contact-loaded-at]");
        const jsEnabled = contactForm.querySelector("[data-contact-js]");
        const requestType = contactForm.querySelector("[data-contact-request-type]");
        const dateField = contactForm.querySelector("[data-contact-date]");
        const dateRequiredMarker = contactForm.querySelector("[data-date-required-marker]");
        const yearField = contactForm.querySelector("[data-current-year]");
        const tfpBlock = contactForm.querySelector("[data-contact-tfp]");
        const tfpImages = contactForm.querySelector("[data-contact-tfp-images]");
        const status = contactForm.querySelector("[data-contact-status]");
        const submit = contactForm.querySelector("[data-contact-submit]");
        const params = new URLSearchParams(window.location.search);
        const termin = params.get("termin");

        if (loadedAt instanceof HTMLInputElement) loadedAt.value = String(Date.now());
        if (jsEnabled instanceof HTMLInputElement) jsEnabled.value = "1";
        if (dateField instanceof HTMLInputElement && termin && /^\d{4}-\d{2}-\d{2}$/.test(termin)) {
          dateField.value = termin;
        }

        const syncRequestType = () => {
          const isTfp = requestType instanceof HTMLSelectElement && requestType.value === "tfp";
          if (dateField instanceof HTMLInputElement) {
            dateField.required = !isTfp;
          }
          if (dateRequiredMarker instanceof HTMLElement) {
            dateRequiredMarker.hidden = isTfp;
          }
          if (tfpBlock instanceof HTMLElement) {
            tfpBlock.hidden = !isTfp;
          }
          if (!isTfp && tfpImages instanceof HTMLInputElement) {
            tfpImages.value = "";
          }
        };

        const validateYear = () => {
          if (!(yearField instanceof HTMLInputElement)) return;
          const expectedYear = yearField.dataset.currentYear || String(new Date().getFullYear());
          const enteredYear = yearField.value.trim();
          yearField.setCustomValidity(
            enteredYear === expectedYear
              ? ""
              : enteredYear.length === 0
                ? `Bitte das aktuelle Jahr ${expectedYear} eintragen.`
                : `Bitte als Sicherheitsfrage das aktuelle Jahr ${expectedYear} eintragen.`
          );
        };

        if (requestType instanceof HTMLSelectElement) {
          requestType.addEventListener("change", syncRequestType);
          syncRequestType();
        }

        if (yearField instanceof HTMLInputElement) {
          const expectedYear = new Intl.DateTimeFormat("de-DE", {
            timeZone: "Europe/Berlin",
            year: "numeric",
          }).format(new Date());
          yearField.dataset.currentYear = expectedYear;
          yearField.placeholder = expectedYear;
          yearField.addEventListener("input", () => {
            yearField.value = yearField.value.replace(/\D/g, "").slice(0, 4);
            validateYear();
          });
        }

        if (tfpImages instanceof HTMLInputElement) {
          tfpImages.addEventListener("change", () => {
            const files = Array.from(tfpImages.files || []);
            const invalidFile = files.find((file) => {
              return !["image/jpeg", "image/png", "image/webp"].includes(file.type)
                || file.size > 2 * 1024 * 1024;
            });
            tfpImages.setCustomValidity(
              files.length > 3
                ? "Bitte maximal drei Bilder auswählen."
                : invalidFile
                  ? "Bitte nur JPG, PNG oder WebP mit maximal 2 MB auswählen."
                  : ""
            );
          });
        }

        contactForm.addEventListener("submit", async (event) => {
          event.preventDefault();

          validateYear();
          if (!contactForm.reportValidity()) return;
          if (!(status instanceof HTMLElement) || !(submit instanceof HTMLButtonElement)) return;

          status.dataset.state = "loading";
          status.textContent = "Die Anfrage wird gesendet.";
          submit.disabled = true;

          try {
            const response = await fetch(contactForm.action, {
              method: "POST",
              body: new FormData(contactForm),
              headers: {
                Accept: "application/json",
                "X-Requested-With": "fetch",
              },
            });
            const data = await response.json();

            if (!response.ok || !data.ok) {
              throw new Error(data.message || "Die Anfrage konnte nicht gesendet werden.");
            }

            status.dataset.state = "success";
            status.textContent = data.message || "Vielen Dank. Eure Anfrage ist angekommen.";
            contactForm.reset();
            if (loadedAt instanceof HTMLInputElement) loadedAt.value = String(Date.now());
            if (jsEnabled instanceof HTMLInputElement) jsEnabled.value = "1";
            syncRequestType();
            status.focus();
          } catch (error) {
            status.dataset.state = "error";
            status.textContent = error instanceof Error
              ? error.message
              : "Die Anfrage konnte nicht gesendet werden. Bitte schreibt direkt per E-Mail.";
            status.focus();
          } finally {
            submit.disabled = false;
          }
        });
      }
    </script>
  </body>
</html>

<style>
  :global(html) {
    overflow-x: clip;
  }

  .contact-page {
    overflow-x: clip;
    background: var(--paper);
    color: var(--ink);
  }

  .contact-page main {
    width: min(1180px, calc(100% - 32px));
    margin: 0 auto;
    padding: clamp(34px, 6vw, 82px) 0 clamp(68px, 8vw, 120px);
  }

  .contact-breadcrumb {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    align-items: center;
    margin-bottom: clamp(34px, 6vw, 74px);
    color: #827a72;
    font-family: "Raleway", Arial, sans-serif;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: .16em;
    line-height: 1.8;
    text-transform: uppercase;
  }

  .contact-breadcrumb a {
    color: inherit;
    text-decoration: none;
  }

  .contact-hero {
    display: grid;
    gap: clamp(28px, 5vw, 68px);
    align-items: end;
    margin-bottom: clamp(44px, 8vw, 110px);
  }

  .contact-kicker {
    margin: 0 0 14px;
    color: #7e746b;
    font-family: "Raleway", Arial, sans-serif;
    font-size: clamp(12px, 1.7vw, 14px);
    font-weight: 800;
    letter-spacing: .22em;
    line-height: 1.5;
    text-transform: uppercase;
  }

  .contact-hero h1,
  .contact-aside h2,
  .contact-form h2 {
    margin: 0;
    color: #1b1b1b;
    font-weight: 500;
    line-height: .98;
    letter-spacing: 0;
  }

  .contact-hero h1 {
    max-width: 9.5em;
    font-size: clamp(54px, 15vw, 146px);
  }

  .contact-hero__copy > p:last-child {
    max-width: 650px;
    margin: clamp(22px, 4vw, 34px) 0 0;
    color: #5f5953;
    font-size: clamp(20px, 3.8vw, 30px);
    line-height: 1.45;
  }

  .contact-hero__image {
    margin: 0;
    min-height: 0;
  }

  .contact-hero__image img {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    object-position: 50% 32%;
    background: var(--soft);
  }

  .contact-shell {
    display: grid;
    gap: clamp(28px, 5vw, 62px);
    align-items: start;
  }

  .contact-aside {
    padding-block: clamp(6px, 2vw, 22px);
  }

  .contact-aside h2,
  .contact-form h2 {
    font-size: clamp(38px, 8vw, 74px);
  }

  .contact-aside p,
  .contact-form__header p {
    margin: 18px 0 0;
    color: #655f59;
    font-size: clamp(18px, 2.3vw, 22px);
    line-height: 1.55;
  }

  .contact-aside dl {
    display: grid;
    gap: 16px;
    margin: 30px 0 0;
  }

  .contact-aside dl > div {
    padding-block: 16px;
    border-top: 1px solid var(--line);
  }

  .contact-aside dt {
    color: #81776f;
    font-family: "Raleway", Arial, sans-serif;
    font-size: 12px;
    font-weight: 800;
    letter-spacing: .18em;
    text-transform: uppercase;
  }

  .contact-aside dd {
    margin: 6px 0 0;
    font-size: clamp(20px, 3vw, 28px);
    line-height: 1.25;
  }

  .contact-aside a {
    text-decoration-thickness: 1px;
    text-underline-offset: .2em;
  }

  .contact-secure-link {
    min-height: 52px;
    margin-top: 24px;
    padding: 14px 18px;
    display: inline-flex;
    align-items: center;
    border: 1px solid var(--line);
    color: #1b1b1b;
    font-family: "Raleway", Arial, sans-serif;
    font-size: 12px;
    font-weight: 800;
    letter-spacing: .12em;
    line-height: 1.4;
    text-decoration: none;
    text-transform: uppercase;
  }

  .contact-form {
    padding: clamp(22px, 4vw, 46px);
    border: 1px solid var(--line);
    background: #fbfaf8;
  }

  .contact-form__header {
    margin-bottom: clamp(28px, 5vw, 46px);
  }

  .contact-form fieldset {
    margin: 0;
    padding: clamp(24px, 4vw, 36px) 0;
    display: grid;
    gap: 18px;
    border: 0;
    border-top: 1px solid var(--line);
  }

  .contact-form legend {
    padding: 0 14px 0 0;
    color: #7e746b;
    font-family: "Raleway", Arial, sans-serif;
    font-size: 12px;
    font-weight: 800;
    letter-spacing: .18em;
    text-transform: uppercase;
  }

  .contact-form__grid {
    display: grid;
    gap: 18px;
  }

  .contact-field {
    display: grid;
    gap: 8px;
  }

  .contact-field[hidden] {
    display: none !important;
  }

  .contact-field label,
  .contact-consent {
    color: #242424;
    font-size: clamp(19px, 2.3vw, 22px);
    line-height: 1.35;
  }

  .contact-field p {
    margin: 0;
    color: #706a64;
    font-size: 15px;
    line-height: 1.45;
  }

  .contact-field input,
  .contact-field select,
  .contact-field textarea {
    width: 100%;
    min-height: 56px;
    padding: 14px 16px;
    border: 1px solid #cfc7bd;
    border-radius: 0;
    background: #fff;
    color: var(--ink);
    font: inherit;
    font-size: 18px;
    line-height: 1.35;
  }

  .contact-field textarea {
    min-height: 190px;
    resize: vertical;
  }

  .contact-field input::placeholder {
    color: #948b82;
  }

  .contact-field input:focus,
  .contact-field select:focus,
  .contact-field textarea:focus,
  .contact-consent input:focus-visible,
  .contact-submit:focus-visible,
  .contact-actions a:focus-visible,
  .contact-secure-link:focus-visible {
    outline: 3px solid #1f6688;
    outline-offset: 3px;
  }

  .contact-consent {
    margin-top: 12px;
    display: grid;
    grid-template-columns: 28px 1fr;
    gap: 14px;
    align-items: start;
  }

  .contact-consent input {
    width: 24px;
    height: 24px;
    margin-top: 3px;
    accent-color: #1f6688;
  }

  .contact-status {
    min-height: 28px;
    margin: 22px 0 0;
    color: #605951;
    font-size: 17px;
    line-height: 1.5;
  }

  .contact-status[data-state="success"] {
    color: #245d3d;
  }

  .contact-status[data-state="error"] {
    color: #9d1f2d;
  }

  .contact-actions {
    margin-top: 18px;
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    align-items: center;
  }

  .contact-submit,
  .contact-actions a {
    min-height: 56px;
    padding: 16px 22px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--ink);
    font-family: "Raleway", Arial, sans-serif;
    font-size: 12px;
    font-weight: 800;
    letter-spacing: .16em;
    line-height: 1.3;
    text-decoration: none;
    text-transform: uppercase;
  }

  .contact-submit {
    background: var(--ink);
    color: #fff;
    cursor: pointer;
  }

  .contact-submit:disabled {
    cursor: wait;
    opacity: .62;
  }

  .contact-actions a {
    background: transparent;
    color: var(--ink);
  }

  .contact-form__honeypot {
    position: absolute;
    left: -10000px;
    width: 1px;
    height: 1px;
    overflow: hidden;
  }

  .contact-footer {
    width: min(1180px, calc(100% - 32px));
    margin: 0 auto;
    padding: 26px 0 44px;
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    border-top: 1px solid var(--line);
    color: #7d756d;
    font-family: "Raleway", Arial, sans-serif;
    font-size: 12px;
    font-weight: 800;
    letter-spacing: .16em;
    text-transform: uppercase;
  }

  .contact-footer a {
    text-decoration: none;
  }

  @media (min-width: 760px) {
    .contact-form__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .contact-field--wide {
      grid-column: 1 / -1;
    }
  }

  @media (min-width: 920px) {
    .contact-hero {
      grid-template-columns: minmax(0, 1.15fr) minmax(300px, .72fr);
    }

    .contact-shell {
      grid-template-columns: minmax(260px, .55fr) minmax(0, 1fr);
    }
  }

  @media (max-width: 767px) {
    .contact-page main,
    .contact-footer {
      width: min(100% - 24px, 680px);
    }

    .contact-hero h1 {
      font-size: clamp(48px, 17vw, 82px);
    }

    .contact-form {
      padding: 20px 16px 24px;
    }

    .contact-actions {
      align-items: stretch;
    }

    .contact-submit,
    .contact-actions a {
      width: 100%;
    }
  }
</style>
