---
import {
  mobileNavigationGroups,
  type NavigationItem,
} from "../data/navigation";
import FloatingActionButton from "./FloatingActionButton.astro";

const currentPath = Astro.url.pathname;
const showFloatingAction = Astro.props.showFloatingAction ?? !currentPath.startsWith("/kontakt");

const toTrackingId = (value: string) => value
  .toLowerCase()
  .replace(/ä/g, "ae")
  .replace(/ö/g, "oe")
  .replace(/ü/g, "ue")
  .replace(/ß/g, "ss")
  .replace(/[^a-z0-9]+/g, "_")
  .replace(/^_+|_+$/g, "");

const isCurrent = (item: NavigationItem) => {
  if (item.href === "/") return currentPath === "/";
  if (!item.href.startsWith("/")) return false;

  return currentPath === item.href.split("#")[0];
};
---

<div
  class="mobile-navigation"
  data-mobile-navigation
  data-track-section="mobile_navigation"
  data-content-topic="site_navigation"
  data-user-intent="navigate"
  data-journey-stage="all"
  data-content-format="navigation"
  data-position="mobile_header"
>
  <div class="mobile-navigation__bar">
    <a
      class="mobile-navigation__brand"
      href="/"
      aria-label="Artbild-Fotografie Startseite"
      data-track-event="cta_click"
      data-cta-id="mobile_logo_home"
      data-cta-type="navigation"
      data-section-id="mobile_navigation"
      data-content-topic="site_navigation"
      data-user-intent="navigate_home"
      data-journey-stage="all"
      data-position="mobile_brand"
    >
      <img
        src="/images/logo-artbild-black.png"
        width="1200"
        height="480"
        alt="Artbild Fotografie"
      >
    </a>

    <button
      class="mobile-navigation__toggle"
      type="button"
      aria-expanded="false"
      aria-controls="mobile-navigation-panel"
      aria-label="Menü öffnen"
      data-mobile-navigation-toggle
      data-track-event="cta_click"
      data-cta-id="mobile_menu_toggle"
      data-cta-type="navigation_toggle"
      data-section-id="mobile_navigation"
      data-content-topic="site_navigation"
      data-user-intent="menu_oeffnen"
      data-journey-stage="all"
      data-position="mobile_header"
    >
      <span class="mobile-navigation__toggle-icon" aria-hidden="true">
        <span></span>
        <span></span>
        <span></span>
      </span>
      <span class="visually-hidden">Menü</span>
    </button>
  </div>

  <div
    class="mobile-navigation__panel"
    id="mobile-navigation-panel"
    data-mobile-navigation-panel
    hidden
  >
    <button
      class="mobile-navigation__backdrop"
      type="button"
      aria-hidden="true"
      data-mobile-navigation-close
      tabindex="-1"
    ></button>

    <div class="mobile-navigation__drawer">
      <header class="mobile-navigation__drawer-header">
        <div>
          <p>Menü</p>
          <span>Artbild-Fotografie</span>
        </div>
        <button
          class="mobile-navigation__close"
          type="button"
          aria-label="Menü schließen"
          data-mobile-navigation-close
        >
          <span aria-hidden="true"></span>
        </button>
      </header>

      <nav class="mobile-navigation__content" aria-label="Mobile Hauptnavigation">
        {mobileNavigationGroups.map((group) => (
          <section
            class:list={[
              "mobile-navigation__group",
              { "is-secondary": group.secondary },
            ]}
            aria-labelledby={`mobile-navigation-${group.label.toLowerCase()}`}
            data-track-section={`mobile_navigation_${toTrackingId(group.label)}`}
            data-content-topic={group.contentTopic ?? (group.secondary ? "hochzeitsgalerien" : "site_navigation")}
            data-user-intent={group.userIntent ?? (group.secondary ? "hochzeitsgalerien_ansehen" : "navigate")}
            data-journey-stage={group.journeyStage ?? (group.secondary ? "auswahl" : "all")}
            data-content-format="navigation_group"
            data-position={group.secondary ? "mobile_secondary" : "mobile_primary"}
          >
            <h2 id={`mobile-navigation-${group.label.toLowerCase()}`}>{group.label}</h2>
            <ul>
              {group.items.map((item) => (
                <li>
                  <a
                    class:list={[
                      "mobile-navigation__link",
                      item.emphasis && `is-${item.emphasis}`,
                      { "is-external": item.external },
                    ]}
                    href={item.href}
                    aria-current={isCurrent(item) ? "page" : undefined}
                    data-track-event={item.external ? "external_resource_click" : "cta_click"}
                    data-cta-id={item.trackingId ?? `mobile_${toTrackingId(item.label)}`}
                    data-cta-type={item.ctaType ?? "navigation"}
                    data-section-id={`mobile_navigation_${toTrackingId(group.label)}`}
                    data-content-topic={item.contentTopic ?? (group.secondary ? "hochzeitsgalerien" : "site_navigation")}
                    data-user-intent={item.userIntent ?? "navigate"}
                    data-journey-stage={item.journeyStage ?? "all"}
                    data-position={`mobile_${toTrackingId(group.label)}_${toTrackingId(item.label)}`}
                  >
                    <span class="mobile-navigation__link-copy">
                      <span class="mobile-navigation__link-title">
                        {item.label}
                        {item.badge && <span class="mobile-navigation__badge">{item.badge}</span>}
                      </span>
                      {item.description && (
                        <span class="mobile-navigation__description">{item.description}</span>
                      )}
                    </span>
                    <span class="mobile-navigation__arrow" aria-hidden="true">
                      {item.external ? "↗" : "→"}
                    </span>
                  </a>
                </li>
              ))}
            </ul>
          </section>
        ))}

        <p class="mobile-navigation__service-area">Hamburg · Norddeutschland · Europa</p>
      </nav>
    </div>
  </div>
</div>

{showFloatingAction && <FloatingActionButton />}

<script>
  const navigationRoots = document.querySelectorAll("[data-mobile-navigation]");

  navigationRoots.forEach((root) => {
    const toggle = root.querySelector("[data-mobile-navigation-toggle]");
    const panel = root.querySelector("[data-mobile-navigation-panel]");
    const closeButtons = root.querySelectorAll("[data-mobile-navigation-close]");

    if (!(toggle instanceof HTMLButtonElement) || !(panel instanceof HTMLElement)) return;

    const focusableSelector = 'a[href], button:not([disabled]), [tabindex]:not([tabindex="-1"])';

    const closeMenu = () => {
      panel.classList.remove("is-open");
      toggle.setAttribute("aria-expanded", "false");
      toggle.setAttribute("aria-label", "Menü öffnen");
      document.body.classList.remove("has-mobile-navigation-open");

      window.setTimeout(() => {
        panel.hidden = true;
        toggle.focus();
      }, 240);
    };

    const openMenu = () => {
      panel.hidden = false;
      toggle.setAttribute("aria-expanded", "true");
      toggle.setAttribute("aria-label", "Menü schließen");
      document.body.classList.add("has-mobile-navigation-open");

      window.requestAnimationFrame(() => {
        panel.classList.add("is-open");
        const firstFocusable = panel.querySelector(focusableSelector);
        if (firstFocusable instanceof HTMLElement) firstFocusable.focus();
      });
    };

    toggle.addEventListener("click", () => {
      if (toggle.getAttribute("aria-expanded") === "true") closeMenu();
      else openMenu();
    });

    closeButtons.forEach((button) => button.addEventListener("click", closeMenu));

    panel.querySelectorAll("a[href]").forEach((link) => {
      link.addEventListener("click", () => {
        document.body.classList.remove("has-mobile-navigation-open");
      });
    });

    panel.addEventListener("keydown", (event) => {
      if (event.key === "Escape") {
        closeMenu();
        return;
      }

      if (event.key !== "Tab") return;

      const focusable = [...panel.querySelectorAll(focusableSelector)].filter(
        (element) => element instanceof HTMLElement && !element.hasAttribute("hidden"),
      );
      const first = focusable[0];
      const last = focusable[focusable.length - 1];

      if (!(first instanceof HTMLElement) || !(last instanceof HTMLElement)) return;

      if (event.shiftKey && document.activeElement === first) {
        event.preventDefault();
        last.focus();
      } else if (!event.shiftKey && document.activeElement === last) {
        event.preventDefault();
        first.focus();
      }
    });
  });
</script>
